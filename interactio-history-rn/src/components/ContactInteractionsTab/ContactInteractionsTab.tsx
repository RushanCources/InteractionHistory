import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const ContactInteractions = () => {  
  return (
    <SafeAreaView>
      <Text> Welcome to Contact Interactions</Text>
    </SafeAreaView>
  );
};

export default ContactInteractions;
